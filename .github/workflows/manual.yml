name: Manual workflow

on:
  workflow_dispatch:

jobs:
  check-cores:
    strategy:
      matrix:
        os: [ macos-15-canary, macos-26-canary ]
            
    runs-on: ${{ matrix.os }}

    steps:
      - name: Fastlane
        run: |
            fastlane --version
