name: Manual workflow

on:
  workflow_dispatch:

jobs:
  test:
    strategy:
      matrix:
        os: [ macos-26, macos-26-canary ]
            
    runs-on: ${{ matrix.os }}

    steps:
      - name: Check
        run: |
            echo "Checking"
