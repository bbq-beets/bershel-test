
name: Manual workflow

on:
  workflow_dispatch:

jobs:
  check:
    strategy:
      fail-fast: false
      matrix: 
        os: [ macos-15-xlarge, macos-15-large ]

    runs-on: ${{ matrix.os }}

    steps:
      - name: Check pip
        run: |
          pip3 --version
          which pip3
