name: Manual workflow

on:
  workflow_dispatch:

jobs:
  check-cores:
    strategy:
      matrix:
        os: [ macos-14-canary, macos-15-canary ]
            
    runs-on: ${{ matrix.os }}

    steps:
      - name: List cores number
        run: |
            sysctl -n hw.ncpu
