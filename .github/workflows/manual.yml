name: Manual workflow

on:
  workflow_dispatch:

jobs:
  xcode-simulators:
    strategy:
      matrix:
        os: [macos-13, macos-14, macos-14-large, macos-15]
            
    runs-on: ${{ matrix.os }}

    steps:
      - name: List atr
        run: |
            cd /
            ls -ld
            cd usr
            ls -ld
            cd local
            ls -ld
            cd share
            ls -ld
