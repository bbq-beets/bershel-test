name: Manual workflow

on:
  workflow_dispatch:


jobs:
  test:
    strategy:
      matrix:
        os: [ macos-12, macos-14, macos-14-large ]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Env
        run: |
            cat ~/.bashrc || true
