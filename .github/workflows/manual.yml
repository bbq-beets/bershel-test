name: Manual workflow

on:
  workflow_dispatch:

jobs:
  check-cores:
    strategy:
      matrix:
        os: [ windows-latest ]
            
    runs-on: ${{ matrix.os }}

    steps:
      - name: Test
        run: |
            choco uninstall llvm
            choco install llvm --version 20.1.8
