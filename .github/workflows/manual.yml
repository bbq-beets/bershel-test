name: Manual workflow

on:
  workflow_dispatch:

jobs:
  xcode-simulators:
    strategy:
      matrix:
        include:
          - os: macos-13

            
    runs-on: ${{ matrix.os }}

    steps:
      - name: ENV
        run: printenv
