name: Manual workflow

on:
  workflow_dispatch:

jobs:
  check-cores:
    strategy:
      matrix:
        os: [ macos-15-canary ]
        attempt: [ 1, 2, 3, 4, 5 ]
            
    runs-on: ${{ matrix.os }}

    steps:
    - name: Test
      run: xcrun simctl list devices available
